import DesktopHomeGrid from "@/components/desktop/DesktopHomeGrid";

export const dynamic = "force-dynamic";

/** Public /projects rewrites here on desktop — same ideas grid home. */
const DesktopProjectsPage = () => <DesktopHomeGrid />;

export default DesktopProjectsPage;
